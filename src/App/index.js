import React from 'react';

import StatusBar from '../StatusBar';
import Band from '../Band';

const App = () => (
  <div>
    <StatusBar/>
    <Band/>
    <button>End turn</button>
  </div>
);

export default App;
